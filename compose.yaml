services:
  web:
    image: tooltown:dev
    build:
      context: .
    ports:
      - "${TOOLTOWN_PORT:-8080}:8080"
    volumes:
      - ./static:/app/static:ro
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
