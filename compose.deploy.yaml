services:
  web:
    image: ghcr.io/rwese/tooltown:latest
    restart: unless-stopped
    ports:
      - "${TOOLTOWN_PORT:-8080}:8080"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
